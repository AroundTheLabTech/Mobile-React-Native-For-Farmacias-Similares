import { StyleSheet } from 'react-native';
import { colors } from '../../../../../global-class'; // Importa las variables globales

const _404Styles = StyleSheet.create({
  containerMax: {
    flex: 1,
    backgroundColor: colors.background2,
  },
  container: {
    backgroundColor: colors.background2,
    alignItems: 'center',
  },
});

export default _404Styles;
